# cpanel default page

## introduction

using [tailwindcss] (https://tailwindcss.com/)

## usage

- run `npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch` during development
- to generate css for production, run `npx tailwindcss -o dist/output.css --minify`